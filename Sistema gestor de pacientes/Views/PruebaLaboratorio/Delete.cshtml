@using SGP.Core.Application.ViewModels.PruebaLaboratorio;
@model SavePruebaLaboratorioViewModel;

<div class="container-fluid mt-5">
    <div class="row">
        <div class="col-6 offset-3">
            <div class="card">
                <div class="card-header bg-dark text-light">
                    <h4>¿Está seguro que desea eliminar la prueba de laboratorio @Model.Nombre?</h4>
                </div>
                <div class="card-body">
                    <form method="post" asp-controller="PruebaLaboratorio" asp-action="ConfirmarEliminar">

                        <input type="hidden" asp-for="Id" />

                        <div class="text-center">
                            <button type="submit" class="btn btn-danger">Eliminar</button>
                            <a asp-controller="PruebaLaboratorio" asp-action="Index" class="btn btn-secondary">Cancelar</a>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

